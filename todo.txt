Act as an Expert Laravel Developer and Database Architect. I need to refactor the architecture of my `Habit` and `HabitCategory` models to act as independent "Master Data" entities. 

**Context & Current Flaws:**
Currently, my `habits` and `habit_categories` tables contain a `student_id` column. This is a design flaw because a Habit (like "Wake up early") should be a universal template. Students should relate to these master habits via a Many-to-Many pivot table, not by owning the master record directly. I already have a `habit_user` pivot table in my database that contains `id`, `habit_id`, `student_id`, `color`, `frequency`, and `is_active`.

**What I need you to do:**

**Step 1: Database Migrations Refactor**
Create a new migration (or give me the code to update existing migrations) to:
1. DROP the `student_id` column from the `habits` table.
2. DROP the `student_id` column from the `habit_categories` table.

**Step 2: Eloquent Models & Relationships Integration**
Update the following models with strict, correctly defined relationships:
1. `HabitCategory` Model:
   - Needs a `hasMany` relationship to the `Habit` model.
2. `Habit` Model:
   - Needs a `belongsTo` relationship to `HabitCategory`.
   - Needs a `belongsToMany` relationship to the `User` model (acting as students) using the `habit_user` pivot table. Include `withPivot('color', 'frequency', 'is_active')` and `withTimestamps()`.
3. `User` Model (Student role):
   - Needs a `belongsToMany` relationship to the `Habit` model using the `habit_user` pivot table.

**Step 3: Controller / Assignment Logic Fix**
When a Mentor assigns a Habit to a Student or Classroom (e.g., in the `/mentor/my-classes/{id}/edit` page):
- Ensure the query to fetch available habits retrieves from the master `habits` table (filtering only by active habits and loading their categories).
- Ensure the saving logic uses `attach()`, `sync()`, or `syncWithoutDetaching()` on the student's `habits()` relationship, saving personal settings like `color` into the pivot table, instead of trying to create a new record in the `habits` table.

Please provide the exact PHP codes for the Migrations, Models, and an example of how the assignment logic (attach/sync) should look in the controller/Livewire component.